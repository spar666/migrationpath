/**
 * ⚠️ DELETE THIS FILE — `rm src/__envcheck.test.ts`
 *
 * It was a scratch file used to prove that VITE_CALENDLY_CONSULT_URL was
 * empty (it was, and openScheduler threw as designed). My sandbox was not
 * permitted to remove it afterwards, so it is left here inert rather than
 * silently broken.
 *
 * The real content it grew into lives in `src/e2eSelectors.test.tsx`.
 * Nothing here is worth keeping.
 */

import { describe, it, expect } from 'vitest';

describe('scratch file — safe to delete', () => {
  it('is a placeholder', () => {
    expect(true).toBe(true);
  });
});
